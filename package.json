{
  "name": "aerobim-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Visor y coordinador BIM en el navegador: IFC, nubes de puntos y coordinacion",
  "license": "MIT",
  "workspaces": [
    "packages/bim-core",
    "packages/viewer",
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run build:packages && npm run dev -w apps/web",
    "dev:host": "npm run build:packages && npm run dev -w apps/web -- --host",
    "build:packages": "npm run build -w packages/bim-core && npm run build -w packages/viewer",
    "build": "npm run build:packages && npm run build -w apps/web",
    "test": "npm run test -w packages/bim-core",
    "lint": "oxlint packages/ apps/",
    "lint:fix": "oxlint --fix packages/ apps/",
    "fmt": "prettier --write .",
    "fmt:check": "prettier --check .",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "lefthook": "^2.1.9",
    "oxlint": "^1.68.0",
    "prettier": "^3.5.0",
    "typescript": "^6.0.3"
  }
}
